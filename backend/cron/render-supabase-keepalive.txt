# Render Cron Job settings (Dashboard -> New -> Cron Job)
# Name: entitled-supabase-keepalive
# Schedule: */10 * * * *
# Runtime: Node
# Build Command: cd backend && npm ci
# Start Command: cd backend && npm run keepalive

# Required env vars for the cron job:
# SUPABASE_URL
# SUPABASE_SERVICE_ROLE_KEY
# SUPABASE_KEEPALIVE_TABLE (optional, default: members)
